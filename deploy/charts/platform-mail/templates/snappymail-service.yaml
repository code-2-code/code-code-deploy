apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-mail.fullname" . }}-snappymail
  labels:
    app.kubernetes.io/name: snappymail
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: snappymail
    app.kubernetes.io/instance: {{ .Release.Name }}
